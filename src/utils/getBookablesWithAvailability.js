// const bookablesWithAvailability = getBookablesWithAvailability(bookingFormData, bookables)
// const getBookablesWithAvailability = (bookingFormData, bookables) =>
// bookables.map(bookable =>
// bookable.bookings.reduce((booking, result) =>
// bookingInterval.overlaps(bookingFormDataInterval) ? return false : result
